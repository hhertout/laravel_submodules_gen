/**
 * Dependencies configuration
 * @type {{REACT: string[], SOLID: string[]}}
 */
const DEPENDENCIES = {
  REACT: ['react', 'react-dom', 'react-router-dom'],
  SOLID: ['solid-js', '@solidjs/router'],
};

export default DEPENDENCIES;
